# 國中自然科學模擬動畫

這是一個收集國中物理、數學、化學互動式教學動畫的網站專案。

## 🎯 專案特色

- **單頁架構**：每個動畫都是獨立完整的 HTML 檔案
- **Bootstrap 5.3**：響應式設計，支援各種裝置
- **WCAG 2.2 AA**：完整無障礙支援
- **JSON 資料驅動**：動畫資料與頁面分離，易於維護
- **動態篩選**：依科目、年級快速找到需要的動畫

## 📁 目錄結構（最新架構：部署隔離）

本專案根目錄僅保留文件與部署設定，所有可公開之前端資源集中於 `public/`。Netlify `publish = "public"`。

```
SchScie/
├── README.md                # 專案簡介（不部署）
├── PROJECT_PLAN.md          # 詳細規劃（不部署）
├── netlify.toml             # 部署設定（publish=public）
└── public/                  # 唯一部署根
  ├── index.html           # 首頁（JSON 資料驅動 + 模組化導航）
  ├── assets/              # 共用資源
  │   ├── css/             # common.css / accessibility.css
  │   ├── js/              # theme.js / navigation.js / a11y.js / ui-helpers.js
  │   └── templates/       # navbar.html / footer.html
  ├── data/                # 單一資料來源
  │   └── animations.json  # 動畫 metadata（新增/修改唯一位置）
  ├── physics/             # 物理動畫頁面
  ├── chemistry/           # 化學（預留）
  └── math/                # 數學（預留）
```

> 舊版根層 `index.html`、`physics/`、`math/`、`chemistry/`、`data/` 已清理；請勿在根目錄新增 .html 檔案。

## 🚀 使用方式

### 本地預覽
```bash
# 直接開啟（建議）
open public/index.html

# 或啟動簡易伺服器於 public 目錄
cd public
python3 -m http.server 8000
# 瀏覽 http://localhost:8000
```

### 新增動畫的步驟

#### 1. 建立動畫檔案
在 Gemini 或其他工具生成動畫後，存入對應資料夾：
```bash
# 例如新增一個波的干涉動畫
public/physics/wave-interference.html
```

#### 2. 編輯 `public/data/animations.json`
在 `animations` 陣列中新增項目：

```json
{
  "id": "wave-interference",
  "title": "波的干涉",
  "subject": "physics",
  "subjectName": "理化",
  "grade": "2-1",
  "gradeName": "國二上",
  "chapter": "波動",
  "description": "觀察兩個波源產生的干涉現象。",
  "keywords": ["波動", "干涉", "建設性干涉", "破壞性干涉"],
  "url": "physics/wave-interference.html",
  "status": "active",
  "featured": true,
  "icon": "fa-solid fa-wave-square",
  "addedDate": "2025-11-29",
  "difficulty": "intermediate"
}
```

#### 3. 更新統計資訊
修改 JSON 頂部的 `metadata.totalAnimations` 數字

#### 4. 驗證並測試
```bash
# 驗證 JSON 格式
python3 -m json.tool public/data/animations.json

# 重新載入首頁測試
open public/index.html
```

## 📋 JSON 欄位說明

### 必填欄位 (✅)
- `id`: 唯一識別碼（與檔名對應）
- `title`: 顯示標題
- `subject`: 科目代碼（physics/math/chemistry）
- `subjectName`: 科目中文名稱
- `grade`: 年級代碼（1-1 至 3-2）
- `gradeName`: 年級中文名稱
- `description`: 簡短描述
- `keywords`: 關鍵字陣列
- `url`: 檔案相對路徑
- `status`: active/coming-soon/disabled

### 選填欄位 (⚠️)
- `chapter`: 章節名稱
- `featured`: 是否顯示 NEW 徽章
- `icon`: Font Awesome 圖示類別
- `addedDate`: 新增日期
- `difficulty`: basic/intermediate/advanced

## 🎨 技術規範

### 框架與工具
- Bootstrap 5.3 (CDN)
- Font Awesome 6 (CDN)
- Vanilla JavaScript (無需 jQuery)

### 無障礙標準
- WCAG 2.2 AA 合規
- 完整鍵盤導航
- ARIA 標籤
- 螢幕閱讀器支援
- 色彩對比度 ≥ 4.5:1

### 瀏覽器支援
- Chrome/Edge (最新版)
- Firefox (最新版)
- Safari (最新版)
- iOS Safari
- Chrome Mobile

## 📖 詳細文件

完整的開發規範、程式碼範例、工作流程請參考：
- [PROJECT_PLAN.md](PROJECT_PLAN.md) - 完整專案規劃文件

## 🔧 維護指南

### 停用某個動畫
在 JSON 中將 `status` 改為 `"disabled"`

### 標記為即將推出
在 JSON 中將 `status` 改為 `"coming-soon"`

### 標記為新動畫
在 JSON 中設定 `"featured": true`

### 修改分類徽章
編輯 JSON 中的 `subjects` 陣列

## 🐛 疑難排解

### 頁面顯示「載入失敗」
1. 檢查 `public/data/animations.json` 是否存在
2. 使用 `python3 -m json.tool public/data/animations.json` 驗證 JSON 格式
3. 開啟瀏覽器開發者工具查看 Console 錯誤訊息

### 篩選功能失效
1. 確認所有動畫都有 `data-subject` 和 `data-grade` 屬性
2. 檢查 JavaScript Console 是否有錯誤
3. 確認 `data-animation-id` 屬性已正確設定

### 卡片未顯示
1. 檢查 JSON 中的 `status` 是否為 `"active"`
2. 確認 `subject` 值與 `subjects` 陣列中的 `id` 一致
3. 檢查 `url` 路徑是否正確

## 📊 目前統計

- 總動畫數：6 個
- 物理動畫：6 個
- 數學動畫：0 個
- 化學動畫：0 個

## 🚀 未來計畫

- [ ] 加入全文搜尋功能
- [ ] 實作 PWA 離線支援
- [ ] 加入使用統計
- [ ] 製作動畫縮圖預覽
- [ ] 多語言支援

## 📝 授權

教育用途自由使用

---

**最後更新**: 2025年11月29日  
**版本**: 2.1.0 (public/ 部署隔離與文件同步)
